 import express from 'express'
 import { directorsControllers } from '../controllers/directors.js';
 const router = express.Router();
 
 //del ejemplo
 let directors=[
 
 ];
 
 
 router.get('/',() => {
 
 });